import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tabataMantenimiento.app',
  appName: 'RMantenimiento',
  webDir: 'dist/myapp/browser'
};

export default config;
